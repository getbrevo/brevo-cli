---
"@getbrevo/cli": patch
---

Add Homebrew as an install channel: `brew install getbrevo/tap/brevo`. The
formula in `getbrevo/homebrew-tap` is auto-bumped on every npm release. No CLI
behavior, command, flag, or env var changed.
