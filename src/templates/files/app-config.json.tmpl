{
  "appId": "{{APP_ID}}",
  "appName": "{{APP_NAME}}",
  "version": "{{APP_VERSION}}",
  "logoUri": "{{LOGO_URI}}",
  "distribution_type": "{{DISTRIBUTION}}",
{{#if ui_app}}
  "ui_app": {{UI_APP_JSON}},
  "auth": {}
{{/if}}
{{#if brevo_function}}
  "brevo_function": {}
{{/if}}
{{#if oauth}}
  "auth": {
    "scopes": {{SCOPES_JSON}},
    "redirectUris": {{REDIRECT_URLS_JSON}}
  }
{{/if}}
}
