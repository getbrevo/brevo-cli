{
  "appId": "{{APP_ID}}",
  "appName": "{{APP_NAME}}",
  "version": "{{APP_VERSION}}",
  "logoUri": "{{LOGO_URI}}",
{{#if ui_app}}
  "distribution_type": "{{DISTRIBUTION}}",
  "ui_app": {{UI_APP_JSON}},
  "auth": {}
{{/if}}
{{#if brevo_function}}
  "distribution_type": "{{DISTRIBUTION}}",
  "brevo_function": {}
{{/if}}
{{#if oauth}}
  "distribution_type": "{{DISTRIBUTION}}",
  "auth": {
    "scopes": {{SCOPES_JSON}},
    "redirectUris": {{REDIRECT_URLS_JSON}}
  }
{{/if}}
}
