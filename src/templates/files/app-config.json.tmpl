{
  "app_id": "{{APP_ID}}",
  "app_name": "{{APP_NAME}}",
  "version": "{{APP_VERSION}}",
  "logo_uri": "{{LOGO_URI}}",
  "distribution_type": "{{DISTRIBUTION}}",
  "app_type": "{{APP_TYPE}}",
{{#if ui_app}}
  "ui_app": {{UI_APP_JSON}},
  "auth": {}
{{/if}}
{{#if brevo_function}}
  "brevo_function": {}
{{/if}}
{{#if oauth}}
  "auth": {
    "scopes": {{SCOPES_JSON}},
    "redirect_uris": {{REDIRECT_URLS_JSON}}
  }
{{/if}}
}
